fastapi==0.139.0
# starlette pinned explicitly (normally a fastapi-transitive) so CI's
# pip-audit sees the exact deployed version — 0.41.x carried 8 advisories.
starlette==1.3.1
uvicorn[standard]==0.34.0
asyncpg==0.30.0
bcrypt==4.2.1
# PyJWT replaced python-jose (archived/unmaintained since 2021); HS256 only,
# so no [crypto] extra needed.
PyJWT==2.13.0
httpx==0.28.1
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.32
alembic==1.18.5
sqlalchemy==2.0.51
greenlet==3.5.3
# IANA tz database for zoneinfo — the scheduler fires at wall-clock 14:00
# Europe/Skopje and python:3.12-slim ships no /usr/share/zoneinfo.
tzdata==2025.2
weasyprint==69.0

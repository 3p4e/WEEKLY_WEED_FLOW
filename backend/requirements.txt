# ── Web framework + ASGI/WSGI servers ──────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
gunicorn==23.0.0
pydantic==2.10.3
pydantic-settings==2.6.1
python-multipart==0.0.20
orjson==3.10.12

# ── Config / HTTP ──────────────────────────────────────────────────────────
python-dotenv==1.0.1
httpx==0.27.2
tenacity==9.0.0

# ── Letta stateful agents (KVM4) ───────────────────────────────────────────
letta-client==0.1.137

# ── Letta database (stateful-agent store: Postgres 17 + pgvector) ──────────
# Direct DB access for analytics; primary task capture is via the Letta API.
psycopg[binary]==3.2.3
SQLAlchemy==2.0.36

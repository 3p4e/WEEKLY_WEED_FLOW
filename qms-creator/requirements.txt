# Cannabis EU GMP QMS Creator - Python Dependencies
# Phase 1: Core Automation Engine

# Web Framework & API
fastapi==0.128.0            # Modern web framework for APIs
uvicorn==0.34.0             # ASGI server for FastAPI
gunicorn==21.2.0            # WSGI HTTP server for production
pydantic==2.12.5            # Data validation and settings management
requests==2.32.3            # HTTP library for health checks and external APIs

# Document Processing
python-docx==1.1.0          # Word document manipulation
PyPDF2==3.0.1               # PDF generation/manipulation
reportlab==4.0.7            # PDF creation from scratch
lxml==5.2.1                 # XML/HTML parsing
Pillow==10.1.0              # Image processing for logos and assets

# Data Management
pyyaml==6.0.1               # Config file handling
pandas==2.1.4               # Data analysis & Excel export
openpyxl==3.1.2             # Excel file handling
sqlalchemy==2.0.23          # Database ORM for tracking systems

# Validation & Processing
jsonschema==4.20.0          # YAML validation
python-dateutil==2.8.2      # Date calculations
regex==2023.10.3            # Advanced pattern matching

# CLI & UX
click==8.1.7                # Command-line interface
rich==13.7.1                # Beautiful terminal output
questionary==2.0.1          # Interactive prompts
tqdm==4.66.1                # Progress bars

# Search & Indexing (Phase 2)
whoosh==2.7.4               # Full-text search
faiss-cpu==1.13.2           # Vector similarity search for RAG

# Testing & Quality
pytest==7.4.3               # Testing framework
black==23.12.0              # Code formatting
mypy==1.7.1                 # Type checking

# Security & Rate Limiting
slowapi==0.1.8              # Rate limiting for FastAPI
python-dotenv==1.0.0        # Environment variable management

# Database Migrations & PostgreSQL
alembic==1.12.1             # Database migration tool
psycopg2-binary==2.9.9      # PostgreSQL adapter for Python

# Monitoring & Logging
sentry-sdk==1.40.1          # Error tracking and monitoring
prometheus-fastapi-instrumentator==6.1.0  # Prometheus metrics
python-json-logger==2.0.7   # JSON structured logging

# Stateful Agents (Letta / MemGPT) — the live AI engine.
# The kvm4 Letta *server* is 0.16.8, but the letta-client *package* has no
# 0.16.x line (it goes 0.1.x then 1.x); 1.12.1 is verified working against
# that server via this app's LettaService. Do NOT use ">=0.16.7" — that
# reads as a client version and is misleading.
letta-client>=1.12,<2      # Python SDK for the self-hosted Letta server

# Workflow Orchestration
langgraph>=1.0.0            # Agent workflow orchestration with state graphs
langchain>=0.2.0            # LLM framework for prompt management and chains
langchain-openai>=0.1.8     # OpenAI integration for LangChain
langchain-google-genai>=0.1.0  # Google Gemini integration

# Additional Utilities
httpx==0.28.1               # Async HTTP client for testing
starlette==0.41.3           # ASGI toolkit (dependency of FastAPI)
anyio==4.12.1               # Async I/O compatibility
